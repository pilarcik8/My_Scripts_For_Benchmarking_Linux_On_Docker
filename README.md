just files with scripts for extracting docker stats to txt file and then formating it into csv - using docker stats
its two files because user needs to stop exctraction with ctrl C
to begin just start a single docker container and then copy and past these scripts
